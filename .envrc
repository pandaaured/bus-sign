# TODO: devenv 2.0 replaces direnv with a Rust-based shell that rebuilds in
# the background. Once stable, drop this file and use `devenv shell` directly.
# https://github.com/cachix/devenv/issues/2061
eval "$(devenv direnvrc)"
use devenv
